{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "npm run setup && npm run build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/build/index.js"
    }
  ]
}
